name: Run Tests
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"
      - name: Upgrade pip
        run: |
          pip install --upgrade pip
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      - name: Install discord_lumberjack
        run: |
          pip install .
      - name: Install PyTest
        run: |
          pip install pytest
      - name: Run tests
        env:
          WEBHOOK_URL: ${{ secrets.WebhookURL }}
          BOT_TOKEN: ${{ secrets.BotToken }}
          CHANNEL_ID: ${{ secrets.ChannelID }}
          DISCORD_DM_ID: ${{ secrets.DiscordDMID }}
        run: |
          pytest
